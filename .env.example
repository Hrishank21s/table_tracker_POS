# Copy to .env and fill in a random secret, e.g. `python -c "import secrets; print(secrets.token_urlsafe(32))"`
TRACKER_API_KEY=
# Comma-separated list of allowed browser origins for the POS frontend
TRACKER_CORS_ORIGINS=http://localhost:8000
# Path to the SQLite database file
TRACKER_DB_PATH=tracker.db
